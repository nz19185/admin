import React, { Component } from 'react';
class useradd extends Component {
    constructor(props) {
        super(props);
        this.state = {  };
    }
    render() {
        return (
            <div>这是用户添加</div>
        );
    }
}

export default useradd;